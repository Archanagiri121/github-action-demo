
name: Secret Demo

on:
  workflow_dispatch:

jobs:

  demo:
    runs-on: ubuntu-latest

    steps:

      - name: Use Secret
        env:
          API_KEY: ${{ secrets.DEMO_API_KEY }}

        run: |
          echo "API key is configured"
          echo "Using API key for application authentication"